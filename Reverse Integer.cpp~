class Solution {
public:
    int reverse(int x) {
     
     int tmp; 
     int prod = 1;
     int rev=0;
     while(x)
     {
         rev = rev*10;
         tmp = x%10;
         rev +=tmp;
         x = x/10;
     }
 
     return rev; 
    }
};
